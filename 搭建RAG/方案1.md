用户：
把它整理成md
豆包：
# DNF-PVF文件知识库构建RAG系统并接入Qwen Code CLI 完整实操指南


## 🧰 第一步：环境准备
确保基础运行环境搭建完成，为后续操作提供稳定依赖。

1. **安装Python环境**（推荐版本 ≥ 3.8，以Linux系统为例）：
```bash
sudo apt update 
sudo apt install python3 python3-venv python3-pip
```

2. **创建并激活虚拟环境**：
```bash
python3 -m venv rag_env 
source rag_env/bin/activate  # Windows系统激活命令：rag_env\Scripts\activate
```

3. **安装核心依赖库**：
```bash
pip install langchain langchain_community sentence-transformers faiss-cpu dashscope
```


## 📦 第二步：下载知识库并构建索引
完成知识库拉取与向量索引生成，为检索功能奠定基础。

1. **克隆DNF-PVF知识库仓库**：
```bash
git clone https://github.com/heihuo000/DNF-PVF-file-Reference.git
```

2. **构建知识库索引**：
   - 创建`index_builder.py`脚本（功能：将知识库中.md文件分块、嵌入为向量）；
   - 执行索引构建命令，示例如下：
```bash
python index_builder.py DNF-PVF-file-Reference faiss_index/index.bin
```
   （注：第一个参数为知识库路径，第二个参数为索引文件输出路径）


## 🛠️ 第三步：构建MCP工具链
在`dnf_rag/tools/`目录下创建3个核心脚本，实现“检索-拼接-生成”的核心链路。

- **vector_search.py**：加载Faiss向量索引，接收用户问题后返回最相关的文本片段；
- **prompt_builder.py**：将用户原始问题与`vector_search`返回的检索结果拼接，生成符合Qwen模型要求的Prompt；
- **generate_answer.py**：调用Qwen大模型，传入拼接后的Prompt，生成精准回答。

可将上述3个脚本封装为统一的`rag_server.py`，作为MCP服务的唯一入口，简化调用流程。


## ⚙️ 第四步：配置Qwen Code CLI
在项目根目录创建配置文件，实现RAG服务与Qwen CLI的对接。

1. 创建`.qwen/settings.json`文件，粘贴以下配置：
```json
{
  "contextFileName": "DNF文件知识库总览.md",
  "includeDirectories": ["DNF-PVF-file-Reference"],
  "loadMemoryFromIncludeDirectories": true,
  "mcpServers": {
    "dnfRagServer": {
      "command": "python",
      "args": ["rag_server.py"],
      "cwd": "./dnf_rag",
      "includeTools": ["vector_search", "prompt_builder", "generate_answer"]
    }
  }
}
```


## 🚀 第五步：运行测试
验证整个系统链路是否通畅，输出精准回答。

1. **启动Qwen Code CLI**：
```bash
qwen
```

2. **输入测试问题**（示例）：
```
如何配置装备文件中的视觉音效标签？
```

3. **系统自动执行流程**：
1. 调用`vector_search()`：从知识库索引中检索相关文本块；
2. 调用`prompt_builder()`：拼接问题与检索结果，生成完整Prompt；
3. 调用`generate_answer()`：通过Qwen模型生成基于知识库的精准回答。


## 🧪 可选：调试与扩展建议
- 新增`chunks.txt`文件，保存文本分块内容，便于调试索引构建逻辑；
- 支持多语言嵌入模型（如`all-MiniLM-L6-v2`），适配英文等非中文知识库；
- 增加`toolCallCommand`配置，实现Qwen CLI内部直接调用工具链路，减少外部依赖。


如果需要生成`index_builder.py`和`rag_server.py`的完整可运行代码，或对某一步骤进行细化说明，我可以直接帮你输出落地性极强的执行文件，需要现在处理吗？

内容由 AI 生成
