# 御鬼之极技能修改方案 - 从被动改为主动

## 项目概述

**目标**：将"御鬼之极"技能从被动技能改为主动技能，增加主动释放功能、持续时间、MP消耗、冷却时间等特性。

**技能基本信息**：
- 技能名称：御鬼之极 (Ghost Release)
- 技能ID：10
- 当前类型：被动技能
- 文件路径：`skill/ghostrelease.skl`
- 现有效果：增加基本攻击力和技能攻击力

## 一、技能改造目标

### 1.1 改造前状态
- **类型**：被动技能 (passive)
- **效果**：永久增加基本攻击力和技能攻击力
- **触发**：学习后自动生效
- **消耗**：无

### 1.2 改造后目标
- **类型**：主动技能 (active)
- **效果**：临时大幅增加基本攻击力和技能攻击力
- **触发**：手动释放
- **持续时间**：30-60秒（根据等级）
- **MP消耗**：100-200 MP（根据等级）
- **冷却时间**：60-120秒（根据等级）
- **施法时间**：500-800毫秒
- **视觉效果**：释放时播放特效动画
- **音效**：释放时播放音效

## 二、文件修改清单

### 2.1 核心文件修改

#### A. SKL技能配置文件
**文件路径**：`skill/ghostrelease.skl`
**修改内容**：
- 将技能类型从 `passive` 改为 `active`
- 添加MP消耗配置
- 添加冷却时间配置
- 添加施法时间配置
- 添加持续时间配置

#### B. SQR脚本文件

**1. 新建技能专用脚本**
- **文件路径**：`sqr/character/swordman/ghostrelease/po_ghostrelease.nut`
- **功能**：处理技能的主动释放逻辑

**2. 修改剑士通用函数库**
- **文件路径**：`sqr/character/swordman/swordman_common.nut`
- **修改位置**：在文件末尾添加御鬼之极相关函数
- **添加内容**：
  - 技能触发条件检查函数
  - 技能效果处理函数
  - 与其他技能的交互处理

**3. 创建技能注册文件**
- **文件路径**：`sqr/character/swordman_load_state.nut`
- **功能**：注册技能状态和被动对象
- **主要内容**：
  - 技能状态注册
  - 技能脚本路径绑定
  - 技能ID映射

### 2.2 完整文件清单

#### C. 核心配置文件

**1. SKL技能配置文件**
- **文件**: `skill/ghostrelease.skl`
- **修改**: 将技能类型从`passive`改为`active`，添加主动技能参数
- **重要性**: 必需 - 核心技能定义

**2. SQR脚本文件（详见上方SQR修改部分）**
- **核心脚本**: 5个文件
- **配置脚本**: 2个文件
- **重要性**: 必需 - 技能逻辑实现

#### D. 被动对象系统文件

**3. OBJ被动对象配置**
- **文件路径**: `passiveobject/elesc/bladespirit_skill.obj`
- **功能**: 定义墓碑被动对象的属性和行为
- **对应ID**: 243445
- **重要性**: 必需 - 墓碑实体定义

**4. ATK攻击信息配置**
- **文件路径**: `passiveobject/elesc/attackinfo/tombstone.atk`
- **功能**: 定义墓碑的攻击属性和伤害参数
- **重要性**: 必需 - 伤害系统

#### E. 角色系统文件

**5. CHR角色配置**
- **文件路径**: `character/swordman/swordman.chr`
- **修改位置**: 在`[swordman]`下的`soulbringer`分支
- **添加内容**:
```
[skill info]
	[index]
		244
	[icon pos]
		235	804
[/skill info]
```
- **重要性**: 推荐 - 技能可见性

#### F. 动画系统文件

**6. ANI动画文件**
- **路径**: `animation/character/swordman/`
- **相关文件**: 
  - `passiveobject/character/swordman/animation/tombstone.ani` - 墓碑主体动画
  - `passiveobject/character/swordman/animation/tombstoneglow1.ani` - 墓碑光效1
  - `passiveobject/character/swordman/animation/tombstoneglow2.ani` - 墓碑光效2
- **说明**: 墓碑的视觉效果动画
- **重要性**: 推荐 - 视觉效果

**7. LAY动画层级文件**
- **文件**: `character/swordman/swordman.lay`
- **作用**: 定义角色动画层级、动作映射、状态控制
- **修改**: 可能需要添加墓碑技能相关的动画映射
- **重要性**: 可选 - 动画优化

**8. ACT动作控制文件**
- **文件**: `character/swordman/swordman.act`
- **作用**: 定义技能施放的动作逻辑、攻击判定、AI行为
- **修改**: 可能需要添加墓碑技能的动作定义
- **重要性**: 可选 - 动作控制

#### G. 资源文件

**9. 音效文件**
- **音效**: `SM_STONE_SHOWER` - 技能释放音效
- **说明**: 增强技能释放的音效体验
- **重要性**: 可选 - 音效体验

**10. 图标文件**
- **文件**: `sprite_character_swordman_effect.NPK`
- **说明**: 如果没有技能图标，需要添加到此文件中
- **重要性**: 可选 - 视觉资源

#### H. 文件重要性分级

**必需文件（核心功能）**
1. `skill/ghostrelease.skl` - 技能定义
2. SQR脚本文件（7个） - 逻辑实现
3. `passiveobject/elesc/bladespirit_skill.obj` - 被动对象
4. `passiveobject/elesc/attackinfo/tombstone.atk` - 攻击属性

**推荐文件（完整体验）**
5. `character/swordman/swordman.chr` - 技能树显示
6. ANI动画文件（3个） - 视觉效果

**可选文件（增强体验）**
7. `character/swordman/swordman.lay` - 动画层级
8. `character/swordman/swordman.act` - 动作控制
9. 音效文件 - 音效反馈
10. 图像资源文件 - 视觉资源

## 三、详细修改方案

### 3.1 SKL文件修改

**原始配置示例**：
```
[skill]
	[name] `御鬼之极`
	[explain] `增加基本攻击力和技能攻击力。`
	[type] `passive`
	[max level] `60`
	[icon] `character/swordman/skill_icon/ghostrelease.img`
	[level info]
		[level]
			[required level] `15`
			[required sp] `20`
			[basic attack] `10`
			[skill attack] `10`
```

**修改后配置**：
```
[skill]
	[name] `御鬼之极`
	[explain] `主动释放后，在一定时间内大幅增加基本攻击力和技能攻击力。`
	[type] `active`
	[max level] `60`
	[icon] `character/swordman/skill_icon/ghostrelease.img`
	[cast time] `500`	// 施法时间(毫秒)
	[cool time] `60000`	// 冷却时间(毫秒)
	[mp] `100`		// MP消耗
	[duration] `30000`	// 持续时间(毫秒)
	[level info]
		[level]
			[required level] `15`
			[required sp] `20`
			[cast time] `500`
			[cool time] `60000`
			[mp] `100`
			[duration] `30000`
			[basic attack] `50`	// 增加的基本攻击力
			[skill attack] `50`	// 增加的技能攻击力
		[level]
			[required level] `16`
			[required sp] `20`
			[cast time] `480`
			[cool time] `58000`
			[mp] `105`
			[duration] `31000`
			[basic attack] `55`
			[skill attack] `55`
		// ... 更多等级配置
```

### 3.2 SQR脚本修改

#### A. 新建技能专用脚本 `po_ghostrelease.nut`

**文件路径**：`sqr/character/swordman/ghostrelease/po_ghostrelease.nut`

**脚本内容**：
```squirrel
// 御鬼之极技能检查函数
function checkExecutableSkill_GhostRelease(obj)
{
	if (!obj) return false;
	
	// 检查技能是否在冷却中
	local skill = sq_GetSkill(obj, 10);
	if (!skill || skill.isInCoolTime()) return false;
	
	// 检查MP是否足够
	local level = sq_GetSkillLevel(obj, 10);
	local needMP = sq_GetLevelData(obj, 10, 2, level); // MP消耗
	if (obj.sq_GetMp() < needMP) return false;
	
	// 检查是否已经有BUFF效果
	if (CNSquirrelAppendage.sq_IsAppendAppendage(obj, "character/swordman/appendage/ap_ghostrelease.nut"))
		return false;
	
	return true;
}

// 御鬼之极技能状态设置函数
function onSetState_GhostRelease(obj, state, datas, isResetTimer)
{
	if (!obj) return;
	
	// 播放施法动画
	obj.sq_SetCurrentAnimation(ENUM_ANI_GHOSTRELEASE_CAST);
	
	// 消耗MP
	local level = sq_GetSkillLevel(obj, 10);
	local needMP = sq_GetLevelData(obj, 10, 2, level);
	obj.sq_AddMp(-needMP);
	
	// 开始冷却
	obj.startSkillCoolTime(10, level, -1);
	
	// 播放释放音效
	obj.sq_PlaySound("GHOSTRELEASE_CAST");
}

// 御鬼之极技能动画结束处理函数
function onEndCurrentAni_GhostRelease(obj)
{
	if (!obj) return;
	
	// 添加BUFF效果
	local level = sq_GetSkillLevel(obj, 10);
	local duration = sq_GetLevelData(obj, 10, 4, level); // 持续时间
	local basicAttack = sq_GetLevelData(obj, 10, 0, level); // 基本攻击力
	local skillAttack = sq_GetLevelData(obj, 10, 1, level); // 技能攻击力
	
	// 创建BUFF附加对象
	local appendage = CNSquirrelAppendage.sq_AppendAppendage(obj, obj, 10, false, 
		"character/swordman/appendage/ap_ghostrelease.nut", true);
	
	if (appendage) {
		appendage.sq_SetValidTime(duration);
		appendage.setAppendCauseSkill(BUFF_CAUSE_SKILL, sq_getJob(obj), 10, level);
		appendage.sq_Rewind();
		
		// 设置攻击力增加数值
		appendage.sq_SetData(QA_BASIC_ATTACK, basicAttack);
		appendage.sq_SetData(QA_SKILL_ATTACK, skillAttack);
	}
	
	// 播放BUFF音效
	obj.sq_PlaySound("GHOSTRELEASE_BUFF");
	
	// 返回待机状态
	obj.sq_AddSetStatePacket(STATE_STAND, STATE_PRIORITY_USER, true);
}
```

#### B. 修改剑士通用函数库

**文件路径**：`sqr/character/swordman/swordman_common.nut`

**在文件末尾添加**：
```squirrel
// 御鬼之极技能相关函数
function setState_Swordman_GhostRelease(obj, state, datas, isResetTimer)
{
	if (!obj) return;
	
	switch(state)
	{
		case STATE_GHOSTRELEASE_CAST:
			onSetState_GhostRelease(obj, state, datas, isResetTimer);
			break;
	}
}

function onEndCurrentAni_Swordman_GhostRelease(obj)
{
	if (!obj) return;
	
	local state = obj.sq_GetState();
	if (state == STATE_GHOSTRELEASE_CAST) {
		onEndCurrentAni_GhostRelease(obj);
	}
}

function checkExecutableSkill_Swordman_GhostRelease(obj)
{
	return checkExecutableSkill_GhostRelease(obj);
}
```

#### C. 创建技能注册文件

**文件路径**：`sqr/character/swordman_load_state.nut`

**文件内容**：
```squirrel
// 剑士技能状态注册文件

// 注册被动对象
sq_AddPassiveObject("character/swordman/ghostrelease/po_ghostrelease.nut");

// 加载核心脚本文件
sq_RunScript("character/swordman/swordman_common.nut");

// 注册自定义脚本运行
function runCustomScript_swordman(obj)
{
	// 御鬼之极技能处理
	setState_Swordman_GhostRelease(obj, obj.sq_GetState(), null, false);
}

// 注册技能状态
function registerState_swordman()
{
	// 注册御鬼之极施法状态
	sq_SetStateName(STATE_GHOSTRELEASE_CAST, "STATE_GHOSTRELEASE_CAST");
}

// 初始化函数
function onInitialize_swordman()
{
	registerState_swordman();
}

// 调用初始化
onInitialize_swordman();
```

### 3.3 技能树配置修改

**文件路径**：`character/swordman.chr`

**修改内容**：确保技能ID 10的配置与新的主动技能类型匹配。

## 四、实施步骤

### 4.1 准备阶段
1. **备份原始文件**
   - 备份 `skill/ghostrelease.skl`
   - 备份 `sqr/character/swordman/swordman_common.nut`

### 4.2 实施阶段

#### 步骤1：修改SKL文件
1. 打开 `skill/ghostrelease.skl`
2. 将 `[type] passive` 改为 `[type] active`
3. 添加施法时间、冷却时间、MP消耗、持续时间配置
4. 调整各等级的数值平衡

#### 步骤2：创建SQR脚本
1. 创建目录 `sqr/character/swordman/ghostrelease/`
2. 新建 `po_ghostrelease.nut` 文件
3. 实现技能检查、状态设置、动画结束处理函数

#### 步骤3：修改通用函数库
1. 打开 `sqr/character/swordman/swordman_common.nut`
2. 在文件末尾添加御鬼之极相关函数

#### 步骤4：创建技能注册文件
1. 新建 `sqr/character/swordman_load_state.nut`
2. 添加被动对象注册和状态注册代码

#### 步骤5：测试验证
1. 进入游戏测试技能释放
2. 验证MP消耗、冷却时间、持续时间
3. 检查BUFF效果是否正常
4. 测试技能升级后的数值变化

### 4.3 验证阶段
1. **功能测试**
   - 技能能否正常释放
   - MP消耗是否正确
   - 冷却时间是否生效
   - BUFF持续时间是否准确
   - 攻击力增加效果是否正常

2. **兼容性测试**
   - 与其他技能的兼容性
   - PVP环境下的表现
   - 不同等级下的效果

3. **错误处理测试**
   - MP不足时的处理
   - 冷却中时的处理
   - BUFF重复释放的处理

## 五、风险评估与预防

### 5.1 技术风险
- **格式错误**：严格遵循PVF文件格式规范
- **脚本错误**：参考现有技能脚本的实现方式
- **状态冲突**：确保新状态ID不与现有状态冲突

### 5.2 平衡性风险
- **数值过强**：合理设置攻击力增加幅度
- **冷却过短**：确保技能不会过于频繁使用
- **持续过长**：避免BUFF时间过长影响游戏平衡

### 5.3 预防措施
- **分步测试**：每个步骤完成后立即测试
- **备份机制**：保留原始文件备份
- **格式检查**：使用PVF工具验证文件格式
- **兼容性测试**：在不同环境下测试

## 六、成功标准

### 6.1 基本功能
- ✅ 技能可以正常释放
- ✅ MP消耗正确
- ✅ 冷却时间生效
- ✅ BUFF效果正常
- ✅ 持续时间准确

### 6.2 用户体验
- ✅ 释放动画流畅
- ✅ 音效播放正常
- ✅ 视觉效果清晰
- ✅ 操作响应及时

### 6.3 系统稳定性
- ✅ 不会导致游戏崩溃
- ✅ 不会影响其他技能
- ✅ 内存使用正常
- ✅ 网络同步正确

## 七、后续优化方向

### 7.1 功能增强
- 添加技能连击效果
- 增加特殊视觉效果
- 实现技能升级动画

### 7.2 平衡调整
- 根据测试反馈调整数值
- 优化冷却时间和持续时间
- 平衡MP消耗

### 7.3 扩展功能
- 添加技能特效强化
- 实现技能组合效果
- 增加PVP专用平衡

---

**注意事项**：
1. 所有字符串必须使用反引号 `` 包围，不能使用双引号 ""
2. 缩进必须使用Tab键，不能使用空格
3. 修改前务必备份原始文件
4. 严格按照PVF文件格式规范进行修改
5. 测试时注意观察游戏日志，及时发现和解决问题

**参考资料位置**：
- `C:\Users\sen\Desktop\nut脚本\86nut\DNF文件知识库\参考资料\sample(官方注释)`
- `C:\Users\sen\Desktop\nut脚本\86nut\DNF文件知识库\PVF文件格式规范.md`
- `C:\Users\sen\Desktop\nut脚本\86nut\DNF文件知识库\剑士技能注册线路图.md`